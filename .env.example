# Malta GPG SDK Configuration

# API Credentials
GPG_API_KEY=your_api_key_here

# Test Mode (true for sandbox, false for production)
GPG_TEST_MODE=true

# Webhook Configuration
GPG_WEBHOOK_SECRET=your_webhook_secret_here

# Optional: Custom timeout in seconds (default: 30)
GPG_TIMEOUT=30